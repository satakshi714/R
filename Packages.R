#installation of packages
install.packages("deplyr")  #case sensitive
library(dplyr)  #load package
require(dplyr)

#get working directory
getwd()

#set working directory
setwd() #paste the required path in double quotes with double backslash'//' instead of single'/' or forward slash '\' can be used
#/- escape character


